/** Ads disabled — no third-party ad scripts or slots are rendered. */
export default function GoogleAdSlot(_props: {
  type?: string;
  className?: string;
  hideAds?: boolean;
}) {
  return null;
}
