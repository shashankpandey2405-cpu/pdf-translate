[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/translate-mt/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
