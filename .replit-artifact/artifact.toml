kind = "web"
previewPath = "/"
title = "PDFTrusted"
version = "1.0.0"
id = "artifacts/pdftrusted"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24178

[services.development]
run = "pnpm --filter @workspace/pdftrusted run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pdftrusted", "run", "build" ]
publicDir = "artifacts/pdftrusted/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24178"
BASE_PATH = "/"
