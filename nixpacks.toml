# Used by Railway translate-worker (and ai worker): run queue consumer only, not Next.js build.
[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["echo pdftrusted-worker: skip next build"]
